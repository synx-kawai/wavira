---
# Wavira Kubernetes Namespace
# Creates an isolated namespace for all Wavira components
apiVersion: v1
kind: Namespace
metadata:
  name: wavira
  labels:
    app.kubernetes.io/name: wavira
    app.kubernetes.io/component: infrastructure
    app.kubernetes.io/managed-by: kustomize
  annotations:
    description: "Wi-Fi CSI based person re-identification and crowd estimation system"
