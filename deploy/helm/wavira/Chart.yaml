apiVersion: v2
name: wavira
description: A Helm chart for Wavira - Wi-Fi CSI based person re-identification and crowd estimation system

type: application

# Chart version - update with each change to the chart
version: 0.1.0

# Application version - matches the Wavira project version
appVersion: "1.0.0"

keywords:
  - wavira
  - csi
  - wifi
  - iot
  - mqtt
  - esp32
  - crowd-estimation

home: https://github.com/your-org/wavira

sources:
  - https://github.com/your-org/wavira

maintainers:
  - name: Wavira Team
    email: wavira@example.com

dependencies:
  # Optional: Use Bitnami Mosquitto instead of our own deployment
  # - name: mosquitto
  #   version: "1.x.x"
  #   repository: "https://charts.bitnami.com/bitnami"
  #   condition: mosquitto.external

annotations:
  category: IoT
  licenses: MIT
